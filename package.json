{
  "name": "gotbilled",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate": "wrangler d1 execute gotbilled-db --remote --file=migrations/0001_initial_schema.sql",
    "db:migrate:local": "wrangler d1 execute gotbilled-db --local --file=migrations/0001_initial_schema.sql"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240909.0",
    "typescript": "^5.6.0",
    "wrangler": "^3.78.0"
  }
}
